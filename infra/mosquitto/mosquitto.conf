# Local compose only. allow_anonymous is forbidden on the VPS
# (see the milestone:season Mosquitto issue).
persistence true
persistence_location /mosquitto/data/

# Bind all interfaces *inside* the container. Host exposure is
# 127.0.0.1 only, via compose ports.
listener 1883
protocol mqtt

listener 9001
protocol websockets

allow_anonymous true
